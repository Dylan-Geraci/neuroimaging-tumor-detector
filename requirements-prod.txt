# Minimal production dependencies for inference only
# This file excludes training, testing, and development dependencies

# Core ML - CPU only, lightweight
torch>=2.0.0
torchvision>=0.15.0
numpy<2.0.0

# Image processing (required for inference)
pillow>=10.0.0
opencv-python-headless>=4.8.0  # headless = no GUI, smaller

# Grad-CAM for heatmap visualization
grad-cam>=1.5.0

# Web application
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Configuration
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Database (for prediction logging)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Security & Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Rate Limiting
slowapi>=0.1.9

# Logging
python-json-logger>=2.0.7

# Model Hosting
huggingface-hub>=0.20.0
